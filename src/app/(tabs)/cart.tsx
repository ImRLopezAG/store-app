import { Wrapper } from '@components/wrapper';
import { Text } from 'react-native';

export default function Cart() {
  return (
    <Wrapper title="Cart">
      <Text>Cart</Text>
    </Wrapper>
  );
}
